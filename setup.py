from setuptools import setup, find_packages

setup(name='treesample',
      version='1.0',
      description='Tree Sampling',
      author='Ran Zmigrod',
      url='https://github.com/rycolab/treesample',
      install_requires=[
          'numpy',
      ],
      packages=find_packages(),
      )
